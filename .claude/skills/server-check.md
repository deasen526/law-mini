# 服务器巡检 Skill

## 触发
用户说"检查服务器"、"服务还在跑吗"、"巡检"

## 流程
1. `curl http://localhost:3000/health`
2. 如果返回 `{"status":"ok"}` → 报告正常
3. 如果失败 → `cd server && node app.js` 重启
4. 再次验证 → 报告结果

## 输出
```
✅ 服务器: 正常运行
📡 端口: 3000
🕐 检查时间: [当前时间]
```
